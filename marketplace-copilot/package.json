{
  "name": "marketplace-copilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser extension that appraises the Marketplace listing you're already looking at.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node --test --experimental-strip-types \"test/*.test.ts\"",
    "analyze": "node --experimental-strip-types src/cli/analyze.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^22.20.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
